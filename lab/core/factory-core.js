window.OCARINA_FACTORY={version:'2.0',outputs:{master:{label:'MASTER',w:3000,h:2250,type:'svg'},whatsapp:{label:'WHATSAPP',w:1080,h:1080,q:.84,type:'jpg'},instagram:{label:'INSTAGRAM',w:1080,h:1350,q:.86,type:'jpg'},story:{label:'STORY / REEL',w:1080,h:1920,q:.84,type:'jpg'},facebook:{label:'FACEBOOK',w:1200,h:630,q:.86,type:'jpg'}},slug(s){return String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')},safeSku(s){return String(s||'').trim().toUpperCase().replace(/[^A-Z0-9_-]+/g,'-')},filename(sku,name,kind,ext){return `${this.safeSku(sku)||'OC-LAB-PRODUCTO'}_${this.slug(name)||'pieza'}_${kind}.${ext}`},download(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1500)},validateProduct(p){const errors=[];if(!p?.sku)errors.push('Falta SKU');if(!p?.name)errors.push('Falta nombre');if(!p?.file&&!p?.files?.length)errors.push('Falta master');if(p?.status!=='available')errors.push('No está disponible');return{ok:!errors.length,errors}},svgToJpg(svg,w,h,q,name){return new Promise((resolve,reject)=>{const img=new Image();const url=URL.createObjectURL(new Blob([svg],{type:'image/svg+xml;charset=utf-8'}));img.onload=()=>{try{const c=document.createElement('canvas'),ctx=c.getContext('2d');c.width=w;c.height=h;ctx.fillStyle='#FFFDF8';ctx.fillRect(0,0,w,h);const scale=Math.max(w/img.width,h/img.height),dw=img.width*scale,dh=img.height*scale;ctx.drawImage(img,(w-dw)/2,(h-dh)/2,dw,dh);c.toBlob(b=>{URL.revokeObjectURL(url);if(!b)return reject(new Error('No se pudo exportar JPG'));this.download(b,name);resolve(name)},'image/jpeg',q)}catch(e){URL.revokeObjectURL(url);reject(e)}};img.onerror=()=>{URL.revokeObjectURL(url);reject(new Error('Master SVG inválido o inaccesible'))};img.src=url})},async exportAll(svg,sku,name){const done=[];const master=this.filename(sku,name,'MASTER','svg');this.download(new Blob([svg],{type:'image/svg+xml;charset=utf-8'}),master);done.push(master);for(const[key,o]of Object.entries(this.outputs)){if(key==='master')continue;const n=this.filename(sku,name,key.toUpperCase(),'jpg');await this.svgToJpg(svg,o.w,o.h,o.q,n);done.push(n)}return done}};